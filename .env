# Note: Values present in the environment at runtime will always override those defined inside the .env file. Similarly, values passed via command-line arguments take precedence as well.
# Each line in an env file to be in VAR=VAL format. 
# Lines beginning with # (i.e. comments) are ignored, as are blank lines.
# Every Service has a separate set of variables.
# Priority of env variables in docker world (1.Compose file 2.Shell environment variables 3.Environment file 4.Dockerfile )

ConnectionStrings__rabbitmq=amqp://guest:guest@rabbitmq:5672
ConnectionStrings__redis=redis:6379
ConnectionStrings__smartfleets=Server=mssql;Database=smartfleets;User Id=sa;Password=yourStrong(!)Password;MultipleActiveResultSets=true;encrypt=False;
ConnectionStrings__consul=http://consul:8500/
