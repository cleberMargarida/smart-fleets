namespace SmartFleets.Application
{
    /// <summary>
    /// Represents a reference to the assembly containing application logic.
    /// This is used for assembly scanning or other reflection-based operations.
    /// </summary>
    public sealed record AssemblyReference();
}
